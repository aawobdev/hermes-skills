The curated learnings file is ready. It filters the local memory to keep only facts useful to any AI coding agent:

- **User section**: Alistair's preferences, GitHub handle, credentials storage (Bitwarden), Slack formatting, Google account
- **Projects & Environment section**: Project locations, homelab setup, Ollama server (with the important note to always query `/api/tags`), Graylog, Google Drive MCP, Plex sharing
- **Conventions section**: Key behaviors like "never assume Ollama models from memory" and "offer actionable next steps"

Removed:
- Hermes-specific tool syntax and script paths
- Cron job SHAs and specific timestamps
- Task progress and session outcomes
- Docker exec commands and detailed operational procedures

The file is now awaiting your approval to write.
